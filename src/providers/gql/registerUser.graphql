mutation registerUser($username: String!,$password: String!, $name:String!,$email: String!, $phone: String )
{
    registerUser(input: {username:$username ,password:$password, name:$name,email:$email, phone:$phone})
    {
        status
        id
        email
    }
}